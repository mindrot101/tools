-- Database initialization placeholder.
-- Schema is created by Sequelize sync(); the default admin user is seeded
-- idempotently by the backend on startup (see server.js). Intentionally a no-op.
SELECT 1;
